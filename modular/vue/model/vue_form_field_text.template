<el-col :span="24">
    <el-form-item :label-width="labelWidth" prop="${fieldName}" label="${fieldDescribe}">
          <el-input v-model="model.${fieldName}" show-word-limit maxlength="${fieldLength}" placeholder="请输入${fieldDescribe}" type="textarea" :rows="3"></el-input>
    </el-form-item>
</el-col>